BITS 64
default rel

extern user_main

section .text
global _start

_start:
    mov ax, 0x23
    mov ds, ax
    mov es, ax

    call user_main

    mov edi, eax
    mov eax, 2
    int 0x80

.hang:
    jmp .hang

section .note.GNU-stack noalloc noexec nowrite progbits
